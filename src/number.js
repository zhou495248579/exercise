export default class Number {
    constructor(props) {
        const div = document.createElement('div');
        div.id = 'number';
        div.innerText = '100';
        document.body.appendChild(div);
    }

}
